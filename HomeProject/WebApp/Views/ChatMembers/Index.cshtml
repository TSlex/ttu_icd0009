@model IEnumerable<Domain.ChatMember>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Profile)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ChatRole)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ChatRoom)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Profile.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChatRole.ChatRoleId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChatRoom.ChatRoomId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ChatMemberId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ChatMemberId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ChatMemberId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
