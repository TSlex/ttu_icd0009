@model IEnumerable<Domain.BlockedProfile>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Profile)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BProfile)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Profile.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BProfile.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BlockedProfileId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BlockedProfileId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BlockedProfileId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
